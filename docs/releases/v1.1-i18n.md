# v1.1 — Internationalization

> Status: **✅ Shipped 2026-05-20**  
> Estimated effort: L (large — 2–4 weeks)  
> Depends on: nothing — can start immediately after v1.0.0

---

## Goal & Scope

Ship the first language beyond English. German is the initial translation target, chosen for its large self-hosting community and broad character coverage (umlauts, ß, compound nouns). The real deliverable is the i18n framework: once the infrastructure and string catalogue are in place, community contributors can add further languages via a documented PR process. RTL layout support is included in this milestone so it does not have to be retrofitted later when Arabic, Hebrew, or Persian translations arrive.

---

## Milestone Checklist

### i18n Foundation

- [x] Install and configure `next-intl` (App Router integration, server + client components)
- [x] Middleware locale detection: `Accept-Language` header, user preference override, cookie-based (no URL prefix — avoids restructuring app directory)
- [x] `messages/en.json` — canonical source-of-truth key structure, ICU MessageFormat throughout
- [x] ICU pluralization rules verified for English: `{count, plural, one {# item} other {# items}}`
- [ ] Date/time and number formatting via `next-intl` locale-aware formatters — **deferred to v1.2** (some dates still use `toLocaleDateString()` directly; see `docs/deferred.md`)
- [x] Locale enum added to Prisma schema (used by user preference field below)

### String Extraction

- [x] Audit all `.tsx` components for hardcoded user-visible strings — 945 keys extracted
- [ ] Audit all server actions and API error messages for user-visible strings — **deferred to v1.2**
- [x] Replace every hardcoded string with a `t('key')` call (all 15+ components and pages)
- [x] Key naming convention: `namespace.key` (e.g., `settings.appearance`, `sidebar.feeds`)
- [x] Pluralization: all count-bearing strings use ICU plural syntax

### German Translation

- [x] `messages/de.json` — complete coverage of all 945 keys in `en.json` (`pnpm run translations:check` ✅)
- [ ] Native-speaker review pass — **community contribution welcome** (see `docs/contributing-translations.md`)
- [x] Zero English fallthrough — fallback to `en.json` only on missing key, never silently
- [ ] Locale-specific date formatting (DD.MM.YYYY) — **deferred to v1.2**, tracked in `docs/deferred.md`

### RTL Layout Completion

- [x] Icon audit: directional icons (`ChevronLeft`/`ChevronRight`, arrows) get `rtl:rotate-180` Tailwind variant
- [x] Replace physical CSS properties with logical equivalents: `ml-`→`ms-`, `mr-`→`me-`, `pl-`→`ps-`, `pr-`→`pe-`, `left-`/`right-` positioning → `start-`/`end-`
- [ ] Swipe gesture direction inverted under RTL — **deferred to v1.2** (see `docs/deferred.md`)
- [ ] Arabic test locale (`ar`) — **deferred**, RTL validation done visually in `layoutDirection: "rtl"` user setting
- [x] Sidebar, article list, reader view, settings, modals: logical properties applied

### Language Settings

- [x] `User.uiLanguage` field added to Prisma schema
- [x] Migration written and applied
- [x] Language picker in Settings → Appearance
- [x] Preference persisted via server action
- [x] Unauthenticated pages respect browser `Accept-Language`

### Community Translations Workflow

- [x] `docs/contributing-translations.md` — complete contributor guide
- [x] GitHub PR template for translation PRs (`.github/PULL_REQUEST_TEMPLATE/translation.md`)
- [x] `scripts/check-translations.mjs` — reports missing/extra keys per locale, CI-compatible (`exit 1` on failure)
- [ ] Translation coverage badge in `docs/contributing-translations.md` — **deferred** (dynamic badge needs CI integration)

### Admin Default Language

- [x] Server Management UI: "Default UI Language" picker in Registrations tab
- [x] Stored in `GlobalSettings.defaultUiLanguage`
- [x] Applied as fallback for new users and unauthenticated pages

### Email Templates

- [x] Digest email subject, headers, dates, footer — locale-aware per recipient `uiLanguage`
- [x] Sign-in (magic link) email — subject, body, button, footer translated
- [x] Fallback to `en` if no language preference
- [x] `lib/email-i18n.ts` — server-side translator reading `messages/*.json` without React
- [ ] 2FA and account deletion confirmation emails — **deferred to v1.2** (low traffic, low impact)

---

## Out of Scope for v1.1

- Languages other than German (framework ships; translations come via community PRs)
- Machine-translation CI pipeline (e.g., DeepL auto-suggestions in PRs)
- Per-feed language override for content display
- Translated AI summary prompts (stays English for now)
- In-app translation editor UI

---

## Dependencies / Blockers

None. Work can begin immediately on the v1.0.0 codebase.

The `next-intl` library supports Next.js App Router and is compatible with the current React 19 + Next.js 16 stack.

---

## Estimated Effort

**L — Large (2–4 weeks)**

The string extraction pass is the longest phase. German translation and RTL work can proceed in parallel once `en.json` is stable. Community workflow docs are a half-day task at the end.
